import { ASTProgram } from './ast';
import { TypeOrigin } from '../types/types';
export declare function parse(src: string, path: string, origin: TypeOrigin): ASTProgram;
export declare function parseImports(src: string, path: string, origin: TypeOrigin): string[];

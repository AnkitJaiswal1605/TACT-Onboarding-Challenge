import { Cell } from "../boc/Cell";
import { Message } from "./Message";
export declare class CommentMessage implements Message {
    readonly comment: string;
    constructor(comment: string);
    writeTo(cell: Cell): void;
}

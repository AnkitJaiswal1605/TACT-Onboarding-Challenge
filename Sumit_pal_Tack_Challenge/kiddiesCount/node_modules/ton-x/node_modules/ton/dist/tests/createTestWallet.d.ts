import { TonClient } from "..";
export declare function createTestWallet(client: TonClient, value: number): Promise<{
    mnemonic: string[];
    key: import("ton-crypto").KeyPair;
    wallet: import("..").Wallet;
}>;

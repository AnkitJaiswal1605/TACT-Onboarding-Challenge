/// <reference types="node" />
import { Address } from "ton";
export declare function extractPublicKeyAndAddress(config: {
    walletType: string;
    walletConfig: string;
}): {
    publicKey: Buffer;
    address: Address;
} | null;

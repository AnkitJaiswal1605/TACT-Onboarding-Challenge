export declare class TonhubHttpTransport implements Transport {
    private readonly _endpoint;
    private readonly _timeout;
    private adapter?;
    constructor(config?: {
        endpoint?: string;
        adapter?: any;
        timeout?: number;
    });
    private getAxiosConfig;
    call(method: string, args: any): Promise<any>;
    createSession(args: any): Promise<any>;
    getSession(args: {
        id: string;
    }): Promise<any>;
    waitSession(args: {
        id: string;
        lastUpdated?: number;
    }): Promise<any>;
    createCommand(args: any): Promise<any>;
    getCommand(args: {
        appk: string;
    }): Promise<any>;
}

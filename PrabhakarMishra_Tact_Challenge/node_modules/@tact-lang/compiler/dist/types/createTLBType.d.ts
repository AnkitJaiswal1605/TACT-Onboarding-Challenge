import { ABIField } from "ton-core";
export declare function createTLBType(name: string, args: ABIField[], kind: 'struct' | 'message', knownHeader: number | null): {
    tlb: string;
    header: number | null;
};

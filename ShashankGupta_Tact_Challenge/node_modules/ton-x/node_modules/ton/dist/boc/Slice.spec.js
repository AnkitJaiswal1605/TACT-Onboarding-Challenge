"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
describe('Slice', () => {
    it('should correctly parse slice', () => {
        const data = Buffer.from('te6cckEBAgEAdwABlQAAAAGABdKDBKoKIeylvKMvkrzd8kx/u3fPQlnrZfq6Osh4kyfgAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAABQ7msoADAEATaAfbgG48KMsJCzkEIf/7nVUBtm8+QWadeayjUryqCULc6juaygAELxkHPM=', 'base64');
        let res = __1.Cell.fromBoc(data)[0];
        const slice = __1.Slice.fromCell(res);
        const seqno = slice.readUint(32);
        const owner = slice.readAddress();
        const seed = slice.readBuffer(32);
        const acceptStakes = slice.readBit();
        const workingCoins = slice.readCoins();
        const lockedCoins = slice.readCoins();
        const nominators = slice.readOptDict(256, (s) => s.readRemaining().buffer.toString('hex'));
        console.warn(seqno);
        console.warn(owner);
        console.warn(seed);
        console.warn(acceptStakes);
        console.warn((0, __1.fromNano)(workingCoins));
        console.warn((0, __1.fromNano)(lockedCoins));
        console.warn(nominators);
        const dict = res.refs[0];
        console.warn((0, __1.parseDictBitString)(dict.beginParse(), 256));
    });
});

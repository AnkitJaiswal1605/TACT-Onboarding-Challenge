import { BitString } from "../BitString";
import { Cell } from "../Cell";
declare type Node<T> = {
    type: 'fork';
    left: Edge<T>;
    right: Edge<T>;
} | {
    type: 'leaf';
    value: T;
};
declare type Edge<T> = {
    label: string;
    node: Node<T>;
};
export declare function buildTree<T>(src: Map<string, T>, keyLength: number): Edge<T>;
export declare function writeLabelShort(src: string, to: BitString): BitString;
export declare function writeLabelLong(src: string, keyLength: number, to: BitString): BitString;
export declare function writeLabelSame(value: number | boolean, length: number, keyLength: number, to: BitString): void;
export declare function detectLabelType(src: string, keyLength: number): "short" | "long" | "same";
export declare function serializeDict<T>(src: Map<string, T>, keyLength: number, serializer: (src: T, cell: Cell) => void): Cell;
export {};

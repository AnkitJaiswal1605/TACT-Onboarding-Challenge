"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const createTestClient_1 = require("../tests/createTestClient");
const ConfigContract_1 = require("./ConfigContract");
describe('ConfigContract', () => {
    it('should execute get methods', async () => {
        const client = (0, createTestClient_1.createTestClient)();
        const contract = new ConfigContract_1.ConfigContract(client);
        // Check seqno method
        // expect((await contract.getSeqNo())).toBeGreaterThan(0);
        // Check that configs are parseable
        let raw = await contract.getConfigsRaw();
        console.warn(raw);
        // Check config typed parsing
        let res = await contract.getConfigs();
        console.warn(res);
    }, 15000);
});

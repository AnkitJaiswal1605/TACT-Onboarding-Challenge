/// <reference types="node" />
import { Cell, ContractSource } from "ton";
import { Maybe } from "ton/dist/types";
export declare class WalletV4Source implements ContractSource {
    static readonly SOURCE: Buffer;
    static create(opts: {
        workchain: number;
        publicKey: Buffer;
        walletId?: Maybe<number>;
    }): WalletV4Source;
    static restore(backup: string): WalletV4Source;
    readonly initialCode: Cell;
    readonly initialData: Cell;
    readonly walletId: number;
    readonly workchain: number;
    readonly publicKey: Buffer;
    readonly type = "org.ton.wallets.v4";
    private constructor();
    describe(): string;
    backup(): string;
}

import { PackageFileFormat, TactLogger } from "./main";
export type VerifyResult = {
    ok: true;
    package: PackageFileFormat;
    files: {
        [key: string]: string;
    };
} | {
    ok: false;
    error: 'invalid-package-format' | 'invalid-compiler' | 'invalid-compiler-version' | 'compilation-failed' | 'verification-failed';
};
export declare function verify(args: {
    pkg: string;
    logger?: TactLogger | null | undefined;
}): Promise<VerifyResult>;
